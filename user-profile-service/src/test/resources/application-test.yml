spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  sql:
    init:
      mode: always

logging:
  level:
    com.gaming.user: DEBUG
    org.springframework.security: WARN
    org.testcontainers: INFO
